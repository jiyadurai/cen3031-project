import React from 'react'

export default function DateSelector({selectedDate, setSelectedDate}) {
    // This compent will allow the user to seelct one date,a range of dates.
    // I may also add a button along with this to increase the day by one.
  return (
    <div>DateSelector</div>
  )
}
